// Names: Store the names of a few of your friends in an array called names. Print each person’s name by accessing each element in the list, one at a time.
let friends_name = ["nabeel", "usman", "ahmer", "rohan"]; // store name of friends
for (let i = 0; i < friends_name.length; i++) {
    const element = friends_name[i];
    console.log(element); //print each name
}
